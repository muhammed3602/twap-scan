[project]
name = "twap-scan"
authors = ["TWAP Oracle Team"]
description = "A Clarity smart contract for tracking Time-Weighted Average Price (TWAP) across decentralized exchanges"
telemetry = false
requirements = []
[contracts.twap-oracle]
path = "contracts/twap-oracle.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
